# user-acc-vue-laravel

## START PROJECT
Copie o arquivo .env.example e crie um arquivo .env com o mesmo conteúdo.

### INSTALL PACKAGES AND RUNNING

install packages
```
composer install
```

running
```
php artisan migrate:fresh --seed && php artisan serve
```
